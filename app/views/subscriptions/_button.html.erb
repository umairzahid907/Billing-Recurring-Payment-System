
<% sub = current_user.subscriptions.find_by(plan_id: plan.id)%>

<% if sub.nil? %>
  <%= link_to 'Subscribe', subscriptions_path(id: plan.id, user_id: current_user.id, plan_id: plan.id), method: :post, remote: true ,class:'btn btn-primary'%>
<% end %>
