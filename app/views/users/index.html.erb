<%= link_to 'Add user', new_user_invitation_path if admin? %>
<br>
<div class='scroll'>
  <% @user.each do |user| %>
    <br>
    <%= link_to 'Show', user_path(user), class:'user-show'%>
    <%= render partial: 'profile', locals: { user: user } %>
  <% end %>
</div>
